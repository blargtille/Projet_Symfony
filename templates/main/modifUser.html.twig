{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Mon profil
{% endblock %}

{% block body %}

    {{ form_start(userForm) }}

    {{ form_widget(userForm) }}

    <button>Modifier</button>
    {{ form_end(userForm) }}

{% endblock %}

