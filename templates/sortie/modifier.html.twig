{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Modifier une sortie
{% endblock %}

{% block body %}
<h1>Modifier une sortie</h1>

    <div>
        {{ form_start(modifySortieForm) }}

        {{ form_widget(modifySortieForm) }}

        <p> Rue : {{ sortie.lieu.rue }} </p>
        <p> Ville : {{ sortie.lieu.ville.nom }} </p>
        <p> Code Postal : {{ sortie.lieu.ville.codePostal }} </p>
        <p> Latitude : {{ sortie.lieu.latitude }} </p>
        <p> Longitude : {{ sortie.lieu.longitude }} </p>

    <div class="button_modifier">
        <button type="submit">Enregistrer</button>
        <button type="submit"><a href="#">Publier la sortie</a></button>
        <button type="submit"><a href="#">Supprimer la sortie</a></button>
        <button type="button"><a href="{{ path('sortie_accueil') }}">Annuler</a></button>
    </div>
        {{ form_end(modifySortieForm) }}
    </div>

{% endblock %}



