{% extends 'base.html.twig' %}

{% block body %}

    <section class="sorties-list">
        {% for sortie in liste %}
            <article>
                <a href="{{ path('sortie_detail', {'id': sortie.id}) }}">
                    {{ sortie.nom }} {{ sortie.dateHeureDebut }} {{ sortie.dateLimiteInscription }} ?/ {{ sortie.nbInscriptionMax }} {{ sortie.etat.nom }}
                    date sortie cloture inscrits/place etat inscrit organisateur actions
                </a>
            </article>
        {% endfor %}



    </section>

{% endblock %}

{% block title %}

    {{ parent() }} | Accueil sorties

{% endblock %}

