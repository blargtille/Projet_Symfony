{% extends 'base.html.twig' %}

{% block body %}

    <h1> Sorties </h1>
    <h2> Filtrer les sorties </h2>


    <!-- formulaire pour filtrer ? ou juste boutons de filtres comme pokemon ? -->

    <form action="#" method="post">
        <label for="site">Site :</label>
       <!-- Recuperer les noms des sites en choices -->
        <select name="listSites" >
            <!-- boucle pour recuperer les sites en bdd ? -->
            <option>un</option>
          <!-- recuperer les sites -->
        </select>

        <label for="recherche">Le nom de la sortie contient :</label>
        <input type="text" name="Rechercher" value="">

        <label for="start">Entre :</label>
        <!-- min date du jour -->
        <input type="date" id="start" name="date-start"
               min="2018-01-01" max="2025-01-01">
        <label for="start">Entre :</label>
        <!-- min date du jour -->
        <input type="date" id="end" name="date-end"
               min="2018-01-01" max="2025-01-01">

        <!-- Dates -->
        </form>

    <!-- Cases à cocher , boutons , form ??-->

        <p>Sorties dont je suis l'organisateur/trice</p>
        <p>Sorties auxquelles je suis inscrit/e</p>
        <p>Sorties auxquelles je ne suis pas inscrit/e</p>
        <p>Sorties passées</p>

    <input type="button" name="Rechercher" value="Rechercher">

    </form>

    <section class="sorties-list">
        <table>
        <tbody>
        <tr>
            <td>Nom de la sortie</td>
            <td>Date de la sortie</td>
            <td>Clôture</td>
            <td>inscrits/places</td>
            <td>Etat</td>
            <td>Inscrit</td>
            <td>Organisateur</td>
            <td>Actions</td>
        </tr>

        {% for sortie in liste %}
            <article>
                <tr>
                <tr href="{{ path('sortie_detail', {'id': sortie.id}) }}">
                    <td>  {{ sortie.nom }} </td>
                    <td>{{ sortie.dateHeureDebut | date('Y-m-d H:i:s') }} </td>
                    <td> {{ sortie.dateLimiteInscription | date('Y-m-d') }}</td>
                    <td> ?/ {{ sortie.nbInscriptionMax }} </td>
                    <td> sortie.etat.nom </td>
                    <td> inscrit ? </td>
                    <td> organisateur ?</td>
                    <td> actions ?</td>
                </tr>
                </a>
            </article>
        {% endfor %}

        </tr>
        </tbody>
        </table>

    </section>

{% endblock %}

{% block title %}

    {{ parent() }} | Accueil sorties

{% endblock %}

